[project]
name = "call-me-maybe"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "argparse>=1.4.0",
    "flake8>=7.3.0",
    "huggingface-hub>=1.29.0",
    "mypy>=2.3.1",
    "numpy>=2.5.2",
    "pydantic>=2.13.5",
    "torch>=2.13.0",
    "transformers>=5.16.1",
]

[project.scripts]
call-me-maybe = "src:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
